{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Portfolio locally",
            "type": "shell",
            "command": "${workspaceFolder}\\mvnw.cmd",
            "args": [
                "spring-boot:run",
                "-Dspring-boot.run.profiles=local"
            ],
            "problemMatcher": [],
            "group": "build"
        }
    ]
}